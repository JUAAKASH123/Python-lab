class rectangle:
    def __init__(self,length,breadth):
        self.length=length
        self.breadth=breadth
    def area(self):
        return self.length*self.breadth
    def perimeter(self):
        return 2*(self.length*self.breadth)
    def compare(self):
        if self.area()==other.area():
           return "Area are same"
        else:
           return"Area are not same"
rec1=rectangle(3,4)
rec2=rectangle(6,5)
print(rec1.area())
print(rec1.perimeter())
print(rec2.area())
print(rec2.perimeter())
